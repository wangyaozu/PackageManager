<Window x:Class="PackageManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PackageManager"
        xmlns:C="clr-namespace:CustomControlLibrary.CustomControl.Controls.DataGrid;assembly=CustomControlLibrary"
        xmlns:controls="clr-namespace:CustomControlLibrary.CustomControl.Controls;assembly=CustomControlLibrary"
        xmlns:button="clr-namespace:CustomControlLibrary.CustomControl.Controls.Button;assembly=CustomControlLibrary"
        mc:Ignorable="d"
        Title="包管理器" Height="700" Width="1300" MinWidth="960"
        ResizeMode="CanResize" WindowStartupLocation="CenterScreen" AllowDrop="True">
    <Window.Resources>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="260" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <controls:CTextBlock Grid.Column="0" Text="产品包管理器" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" />
            <TextBox Grid.Column="1" x:Name="SearchBox" Height="28" Margin="16,0,0,0" Visibility="Hidden" />
            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <button:CButton Content="刷新" Margin="0,0,8,0" Padding="10,6" Click="RefreshButton_Click" />
                <button:CButton Content="设置" Margin="0,0,8,0" Padding="10,6" Click="SettingsButton_Click" />
                <button:CButton Content="路径" Margin="0,0,8,0" Padding="10,6" Click="LocalPathSettingsButton_Click" />
                <button:CButton Content="日志" Padding="10,6" Click="OpenLogViewerButton_Click" />
                <button:CButton Content="产品日志" Margin="8,0,0,0" Padding="10,6" Click="OpenProductLogButton_Click" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="6" />
                <ColumnDefinition Width="450" />
            </Grid.ColumnDefinitions>

            <C:CDataGrid x:Name="PackageDataGrid"
                         Grid.Column="0"
                         EnableAutoTemplateSelection="True"
                         ItemsSource="{Binding Packages}"
                         SelectedItem="{Binding LatestActivePackage, Mode=TwoWay}" />

            <GridSplitter Grid.Column="1"
                          Width="6"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          Background="#22000000" />

            <Border Grid.Column="2" BorderBrush="#1F000000" BorderThickness="1" Padding="12">
                <StackPanel>
                    <controls:CTextBlock Text="{Binding LatestActivePackage.ProductName}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <controls:CTextBlock Text="版本" Width="80" VerticalAlignment="Center" />
                        <controls:CComboBox ItemsSource="{Binding LatestActivePackage.AvailableVersions}"
                                            SelectedItem="{Binding LatestActivePackage.Version, Mode=TwoWay}" Width="320" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <controls:CTextBlock Text="包名" Width="80" VerticalAlignment="Center" />
                        <controls:CComboBox ItemsSource="{Binding LatestActivePackage.AvailablePackages}"
                                            SelectedItem="{Binding LatestActivePackage.UploadPackageName, Mode=TwoWay}" Width="320" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <controls:CTextBlock Text="可执行版本" Width="80" VerticalAlignment="Center" />
                        <controls:CComboBox ItemsSource="{Binding LatestActivePackage.AvailableExecutableVersions}"
                                            DisplayMemberPath="DisPlayName"
                                            SelectedValuePath="DisPlayName"
                                            SelectedValue="{Binding LatestActivePackage.SelectedExecutableVersion, Mode=TwoWay}" Width="320" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                        <button:CButton Content="解锁更新" Command="{Binding LatestActivePackage.UnlockAndDownloadCommand}" Width="80" Margin="0,0,8,0" IsEnabled="{Binding LatestActivePackage.IsEnabled}" />
                        <button:CButton Content="更新" Command="{Binding LatestActivePackage.UpdateCommand}" Width="80" Margin="0,0,8,0" IsEnabled="{Binding LatestActivePackage.IsEnabled}" />
                        <button:CButton Content="打开Revit" Command="{Binding LatestActivePackage.OpenPathCommand}" Width="100" Margin="0,0,8,0" IsEnabled="{Binding LatestActivePackage.IsEnabled}" />
                        <button:CButton Content="切换配置" Command="{Binding LatestActivePackage.ChangeConfigPresetCommand}" Width="80" IsEnabled="{Binding LatestActivePackage.IsEnabled}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                        <button:CButton Content="参数目录" Command="{Binding LatestActivePackage.OpenParameterConfigCommand}" Width="80" Margin="0,0,8,0" IsEnabled="{Binding LatestActivePackage.ConfigOpsEnabled}" />
                        <button:CButton Content="模式切换" Command="{Binding LatestActivePackage.ChangeModeToDebugCommand}" Width="80" Margin="0,0,8,0" IsEnabled="{Binding LatestActivePackage.ConfigOpsEnabled}" />
                        <button:CButton Content="签名加密校验" Command="{Binding LatestActivePackage.RunEmbeddedToolCommand}" Width="120" IsEnabled="{Binding LatestActivePackage.CanRunSignatureEncryption}" />
                        <button:CButton Content="CSV加解密" Width="100" Margin="0,0,8,0" Click="OpenCsvCryptoWindowButton_Click"/>
                    </StackPanel>
                    <controls:CProgressBar Minimum="0" Maximum="100" Value="{Binding LatestActivePackage.Progress}" Height="16" Margin="0,16,0,0" />
                    <controls:CTextBlock Text="{Binding LatestActivePackage.StatusText}" Foreground="#6B6B6B" Margin="0,8,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <StatusBar Grid.Row="2" Margin="0,12,0,0">
            <StatusBarItem>
                <controls:CTextBlock Text="{Binding LatestActivePackage.StatusText, FallbackValue=就绪, TargetNullValue=就绪}" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
