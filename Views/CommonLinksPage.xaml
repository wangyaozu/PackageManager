<Page x:Class="PackageManager.Views.CommonLinksPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:CustomControlLibrary.CustomControl.Controls;assembly=CustomControlLibrary"
             xmlns:button="clr-namespace:CustomControlLibrary.CustomControl.Controls.Button;assembly=CustomControlLibrary"
             xmlns:dataGrid="clr-namespace:CustomControlLibrary.CustomControl.Controls.DataGrid;assembly=CustomControlLibrary"
             mc:Ignorable="d">
    <Grid Background="#00000000">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 面包屑与页标题 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <controls:CTextBlock Text="常用网址导航" FontSize="18" FontWeight="SemiBold" />
        </StackPanel>

        <!-- 说明 -->
        <controls:CTextBlock Grid.Row="1" Text="点击操作列的“打开”在系统默认浏览器中打开链接" Margin="0,0,0,8"/>

        <!-- 链接列表 -->
        <dataGrid:CDataGrid Grid.Row="2"
                            ItemsSource="{Binding Links}"
                            AutoGenerateColumns="False"
                            CanUserAddRows="False"
                            CanUserDeleteRows="False"
                            HeadersVisibility="Column"
                            RowHeight="36"
                            IsReadOnly="True">
            <dataGrid:CDataGrid.Columns>
                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="地址" Binding="{Binding Url}" Width="4*"/>
                <DataGridTemplateColumn Header="操作" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <button:CButton Content="打开"
                                            Padding="10,6"
                                            HorizontalAlignment="Center"
                                            Click="OpenLink_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </dataGrid:CDataGrid.Columns>
        </dataGrid:CDataGrid>

        <!-- 底部返回按钮 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <button:CButton Content="返回" Width="100" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
